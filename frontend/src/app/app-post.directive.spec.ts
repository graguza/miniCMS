import { AppPostDirective } from './app-post.directive';

describe('AppPostDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPostDirective();
    expect(directive).toBeTruthy();
  });
});
